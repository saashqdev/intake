import { CMS } from '../../models';

import { {{ pascalCase name }}Block } from './{{ kebabCase name }}.model';

export const map{{ pascalCase name }} = (
    cms: CMS.Model.{{ pascalCase name }}Block.{{ pascalCase name }}Block,
    locale: string,
): {{ pascalCase name }}Block => {
    return {
        __typename: '{{ pascalCase name }}Block',
        id: cms.id,
    };
};
