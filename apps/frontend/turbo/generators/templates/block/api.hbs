import { Blocks, Headers } from '@o2s/api-harmonization';

import { Sdk } from '@o2s/framework/sdk';

import { getApiHeaders } from '../../utils/api';

const API_URL = Blocks.{{ pascalCase name }}.URL;

export const {{ camelCase name }} = (sdk: Sdk) => ({
    blocks: {
        get{{ pascalCase name }}: (
            query: Blocks.{{ pascalCase name }}.Request.Get{{ pascalCase name }}BlockQuery,
            headers: Headers.AppHeaders,
            authorization: string,
        ): Promise<Blocks.{{ pascalCase name }}.Model.{{ pascalCase name }}Block> =>
            sdk.makeRequest({
                method: 'get',
                url: `${API_URL}`,
                headers: {
                    ...getApiHeaders(),
                    ...headers,
                    Authorization: `Bearer ${authorization}`,
                },
                params: query,
            }),
    },
});
