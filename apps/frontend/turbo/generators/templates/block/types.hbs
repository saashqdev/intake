import { Blocks } from '@o2s/api-harmonization';

export interface {{ pascalCase name }}Props {
    id: string;
    locale: string;
    accessToken?: string;
}

export type {{ pascalCase name }}PureProps = {{ pascalCase name }}Props & Blocks.{{ pascalCase name }}.Model.{{ pascalCase name }}Block;
