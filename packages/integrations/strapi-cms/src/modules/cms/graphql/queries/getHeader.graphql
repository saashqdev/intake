query getHeader($locale: I18NLocaleCode!, $id: ID!) {
    header(locale: $locale, documentId: $id) {
        ...Header
    }
    configurableTexts {
        actions {
            close
        }
    }
}
